{
    IBClasses = (
        {
            ACTIONS = {installDaemon = id; makePlaylist = id; }; 
            CLASS = AppController; 
            LANGUAGE = ObjC; 
            OUTLETS = {sizeField = NSTextField; sizeLabel = NSTextField; window = NSWindow; }; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }
    ); 
    IBVersion = 1; 
}